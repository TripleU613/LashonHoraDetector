# Lashon Hora Detector

This Chrome/Firefox extension blurs Discourse posts that contain blocked phrases and overlays “LASHON HORA.”

## Folders
- `chrome/` MV3 build
- `firefox/` MV2 build

## Release
Push a tag like `v0.1.0` to trigger a release with both zipped builds.
